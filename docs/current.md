# Current state

What runs in the repo today.

## Runnable today

- **Admin portal (`admin/`)** — **ARCHIVED 2026-07-30.** Do not run or deploy. Successor is **`admin-web-app/`**. Keep using [`admin/db/*.sql`](../admin/db/) for shared Supabase migrations. Details: [admin/README.md](../admin/README.md), [admin/ARCHIVED.md](../admin/ARCHIVED.md). Historical feature notes remain under [`docs/admin/`](admin/).
- **Web app** in `admin-web-app/` — Next.js admin UI on **http://localhost:3000** (`npm run dev` in `admin-web-app/`); production on **https://cleanupgiveback-web-app.vercel.app** (Vercel project `cleanupgiveback-web-app`, deployed 2026-07-30). **`/login`** (branded cream gradient-bars background + email/password) is live; `src/middleware.ts` enforces admin claim when `BYPASS_AUTH` is off (`/login` still previewable while bypass is on). Shell: hamburger + overlay below `lg` (1024px), hover icon rail at desktop; tablet layouts stack filter toolbars and use 2-col KPI/chart grids. **Live-wired to the same Supabase project as the mobile `frontend/`** — Dashboard/Sessions/Feedback/Orders/Payments/Users/Volunteers/Events/Insights/Analytics read real `sessions`, `volunteer_feedback`, `shop_orders`, `donations`, `court_orders`, `events`, and Auth-user rows. **Home metric tiles** (Waiting / Approved / Hours / Feedback) show a signed period delta + “vs prior”; Approved/Hours/Feedback follow `PeriodToggle`, Waiting stays live open-queue. **PeriodToggle** presets are Today / Month / Year / All / Custom (30-day removed). **Sessions, Orders, and Feedback** lists (and their KPIs) follow PeriodToggle via `filterByListPeriod` — **Month = rolling last 30 days**. Home / Insights keep calendar month. **Payments** Month = last 6 months (monthly bars), Year = last 6 years (yearly bars). **Feedback** (`/feedback`) has PeriodToggle plus client-side rating filters (All / Excited → Very Sad) and clickable distribution columns. **Sessions, Users/Volunteers, and court progress never fall back to fixtures** (empty → empty real lists). Orders/feedback/payments/revenue still fall back to fixtures + a **Sample data** banner when those tables are empty. Volunteer profile (`/volunteers/[id]`) KPI strip includes **Miles Walked** (sum of approved session `distance_miles`) alongside Sessions and Approved Hours. `/events/new` and `/events/[id]/edit` write real events + photos; event detail supports publish/unpublish/delete and emailing at-risk court-ordered volunteers via Resend (`notifyAtRiskVolunteers`, soft-fails without `RESEND_API_KEY`); `/sessions` supports inline Approve/Decline moderation (writes `sessions` + `admin_audit_log`, emails/pushes the volunteer) plus **bulk approve** via row checkboxes, and renders live `active` (in-progress) rows without crashing; the session preview drawer adds **Hours Adjustment**, **Admin Notes**, **Letterhead PDF** generation (proxies to `backend/sessions` via `SESSIONS_API_URL`/`ADMIN_API_KEY`), and **live Walking Path + Photos** when available (MapLibre polyline from `sessions.route` with Play/Pause/Replay + **fullscreen**, Start/End labels, checkpoint **photo thumbnails along the trail** — prefer stored `checkpoints.latitude`/`longitude` when present, else time-along-route — and signed grid photos from the `session-photos` bucket via `loadSessionEvidence`). Checkpoint GPS columns: run [`admin/db/007_checkpoint_coordinates.sql`](../admin/db/007_checkpoint_coordinates.sql) on Supabase; mobile + Fly API embed coords on new checkpoint submits. Copy `admin-web-app/.env.local.example` → `.env.local` with the shared Supabase project (plus `RESEND_API_KEY`/`EMAIL_FROM`/`DONNA_EMAIL` for mail, optional `SESSIONS_API_URL`/`ADMIN_API_KEY`) to go live. **Resend is live** for local admin + Fly (domain `cleanupgiveback.org` verified 2026-08-03; smoke-tested). Vercel production still needs the same Resend env vars for hosted admin email. Donations revenue reads `public.donations` (`admin/db/006_donations.sql`), falling back to fixtures when empty. **Payments → Revenue share** shows MoM shop % + per-item share-point deltas + top mover. **Realtime session refresh (2026-08-04):** `/sessions` and `/dashboard` subscribe to Supabase Realtime on `public.sessions` (`useSessionsRealtimeRefresh`) and auto-refresh within ~400ms of a mobile session insert/update or another admin's approve/decline — requires `admin/db/008_admin_sessions_realtime_read.sql` applied. **Decision templates, volunteer timeline, readiness page, court packet (2026-08-07):** the session drawer's Decline action now offers hardcoded reason templates + admin-note snippets (`lib/decisionTemplates.ts`); `/volunteers/[id]` gained a chronological Timeline section sourced from `admin_audit_log` (including a new `'email sent'` audit action); `/settings` gained a Production Readiness panel probing Resend/Sessions API/Supabase/storage/Realtime; the Letterhead section gained an "Export Court Packet" link (court-ordered sessions) that adds a cover sheet + adjusted-hours annotations to the existing service-letter PDF. **Attention inbox, court-risk dashboard, communication log, session compare, editable email templates (2026-08-07):** new **`/attention`** page aggregates sessions-under-review, flagged feedback, order issues, failed emails, at-risk volunteers, red-flagged sessions, and data-quality alerts (`lib/attention-inbox.ts`, `lib/data-quality.ts`) into one queue; new **`/court-risk`** page batches the per-volunteer activity-pattern rollup across every court-ordered volunteer with deadline/hours/**missed-checkpoints**/**late-rush** columns — deadline uses a 3-step severity pill (solid dark red overdue > light red due-within-5-days > amber due-within-14-days) rather than a flat binary, and row click opens the volunteer profile (2026-08-09 fix: **Session History** on that profile now opens the same session-preview drawer used elsewhere instead of a dead `/sessions/[id]` link, and court-ordered volunteers no longer crash on load — `CourtOrderForm` had a function prop crossing the server/client boundary); `/volunteers/[id]` gained a **Communication** section (email send log via new `admin/db/010_email_log.sql` `email_log` table, wired into every send site, plus manual contact notes) with an optional Resend delivery-status webhook at `/api/webhooks/resend` (inert until configured in the Resend dashboard); `/sessions` gained a **Compare sessions** mode → `/sessions/compare` for same-volunteer side-by-side photos/route/notes; the Audit Log's before/after diff is now a two-column card (`AuditDiffCard.tsx`) instead of inline text; and **shipped-order email is now sent** (previously no send existed) on the pending/paid → shipped transition. **`/emails` (2026-08-09):** Compose shows read-only **From** (`EMAIL_FROM`), unified To, optional Cc/Bcc, font/size/color, Send or Schedule; schedule send uses a custom date + 15-min time-dropdown picker instead of the browser's `datetime-local` UI; Send/Schedule buttons show an inline hint listing what's missing (recipient/subject/message) when disabled; **Scheduled** tab edit/cancel/send-now (`admin/db/013_scheduled_emails.sql` + cron); Templates use everyday insert chips (no mustache in UI). See [admin-web-app.md](admin-web-app.md).
- **Expo app** in `frontend/` — Expo SDK 54, React Native 0.81, Expo Router
- **HTML prototype mode** — `frontend/src/app/prototype/[screen].tsx` renders Stitch HTML screens in a WebView with JS navigation bridge
- **Entry flow** — Splash → `/welcome` → create-account → `/creating-account` (2 facts, ~7.5s) → account-phone (**full legal name** + phone; accuracy notice) → account-details (birthday; **COPPA `age < 13` clears onboarding PII and routes to `/under-age`**; court acceptance + nighttime ban) → permissions → tour. Home greeting uses legal name from store. Recent events use mock location-mapped remote image URIs. **Register** on event detail emails confirmation when API+Resend configured. Account: company code upgrades tracker (removes free-hour countdown); footer © + 501(c)(3) nonprofit message; **Give Feedback** → `/give-feedback`. Personal details: email-only with OTP. Export: court mandated → approved-only. Session start: form → selfie/progress → live. **End session:** end photos → `/submission-confirmation` (optional **Share Feedback** → `/session-feedback` → `/feedback-thank-you` → back to confirmation). Sessions list: Select + multi-status mocks for bulk delete.
- **Session Tracking** — Production routes under `frontend/src/app/` import `liveSessionStore` + map/checkpoint components from `frontend/src/features/session-tracking/` (GPS, `expo-camera`, Fly API, WebView maps in Expo Go / native MapLibre in EAS). Checkpoint submit resolves WGS84 via `resolveCheckpointCaptureCoords` and posts `latitude`/`longitude` to Fly so admin Walking Path pins use real capture locations. The folder also keeps a **legacy mock** `dev/PreviewApp.tsx` harness for isolated UI review (see the feature's [README.md](../frontend/src/features/session-tracking/README.md)).
- **Home dashboard (`frontend/src/features/figma-screens/screens/HomeScreen.tsx`)** — mounted at `/` with Figma-exact SVG icons, interactive week picker, and shared `BottomNavBar`. Service Hours chart, total hours, streak badge, and Your Impact stats derive from `sessionStatsStore` (local snapshots on session end + Fly API hydration); recent sessions from `recentSessionsStore`. Empty states until first session ends. Recent Sessions **View All** opens `/sessions-list`. **Upcoming Events** load published rows from Supabase `public.events` on focus (`eventsApi.ts`); falls back to mock events when none published; up to three cards (tap opens `/event-detail`). Returning-user populated copy: `HomeScreenReturningUser` + `mocks/home.returningUser.ts` (PreviewApp). **Sessions** tab opens `/sessions-list` (Figma `515:1791`); **Profile** tab opens `/account`. When a live session is active (`liveSessionStore.isActive`), a minimized tracker pill (`LiveSessionMinimizedPill`) shows live elapsed time, checkpoint countdown, distance, an animated checkpoint progress fill, and submitted-checkpoint dots + label only after at least one **early** checkpoint photo (`submittedEarly`); expand (top-right chevron or Track tab) slides the pill down while `/live-session` opens with a bottom-up map reveal wipe; returning to Home slides the pill back up. Live session back chevron (down) minimizes via immediate `dismissTo('/')` (session keeps running; pill stays on Home). **Court Progress card (2026-08-07):** for court-ordered volunteers, a card above Service Hours shows required/approved/remaining hours + due date from a new Fly endpoint `GET /me/court-progress` (`courtProgressStore.ts`); shown when `user_metadata.service_type === 'Court Ordered'` OR the backend reports an active court order, so self-selected court-ordered volunteers see a "not yet set up" state before an admin enters their order.
- **Event detail (`/event-detail`)** — native `EventDetailScreen` (Figma `events_detail`, `196:226`): loads published event by `?id=` from Supabase when available (else mock catalog); hero image carousel from `events.image_urls` (falls back to `image_url` / app default), badges, overview, organizer (logo + Instagram/Facebook/YouTube icons), what to bring (clothing/water/shoes glyphs); copy icon copies Maps link with toast; location section shows a live MapLibre pin preview (WebView in Expo Go, native in dev-client; Maps CTA on web) and opens Apple Maps (iOS) / Google Maps (Android) on tap; **Add to calendar** (calendar glyph + label) via `expo-calendar`; **Register** CTA uses Noto Sans SemiBold and shows `EventRegistrationSuccessModal` (Figma `787:406`); **Go Home** returns to `/`. Event detail icons are `react-native-svg` ports (not `expo-image` SVG requires).
- **Shop (`/shop`)** — native `ShopScreen` (Figma `498:606`): donate card, featured kit, filters, product grid; **Add to cart** → empty-by-default `cartStore`; cart badge = live qty; empty cart icon tap shows toast; cart icon → `/cart` when items exist.
- **Donate (`/donate`)** — native `DonateScreen` (Figma `412:4` / PRD §6.20): Contribute top bar, impact card, amount presets + custom; **Continue** → `/purchase-confirmation?mode=donation&amount=` (donation receipt).
- **Product detail (`/product-detail`)** — native `ProductDetailScreen`: qty + **Add to cart** → `cartStore`; empty-cart toast on cart icon; cart badge live; tote-bags / cleanup-kit / adult + child vest carousels lead with upright product flatlay photos (prior slides retained); tote also includes group photo + all variants (color swatches deselected); Earth/Ocean filter when a swatch is chosen.
- **Cart (`/cart`)** — native `CartScreen`: line items (name + product description preview, 2 lines truncated with ellipsis) + donation from shared `cartStore` (default **$5**); descriptions come from product detail when adding from shop/product detail; **Continue** → `/checkout`.
- **Checkout (`/checkout`)** — native `CheckoutScreen`: order summary from `cartStore`; Place Order validates required fields with red highlights + missing-fields toast; empty-cart toast on cart icon; **Place Order** → `/purchase-confirmation`.
- **Purchase confirmation (`/purchase-confirmation`)** — order mode lists cart lines; donation mode (`?mode=donation`) shows gift-only receipt + “Total Gift”; hearts animate on enter.
- **Sessions list (`/sessions-list`)** — native `SessionsScreen` (Figma `sessions_list___hybrid_redesign`, `515:1791`): search bar, All / Approved / Under Review / Declined chips, sort dropdown with **Select** on the same row; refetches on focus from Fly API when `EXPO_PUBLIC_API_URL` is set; empty list (new users / no logged sessions) shows **No sessions logged yet.** with **Log session?** → `/session-setup-guide`; when API is unset, shows filtered local mock rows; **Select** mode for bulk hard delete of non-approved sessions and **Download PDF** for selected approved sessions; row tap opens `/session-detail?id=` (when not selecting).
- **Shop home (`/shop`)** — native `ShopScreen` (Figma `shop_home`, `498:606` / PRD §6.19): mission copy, donate card ($5/$10/$15/Custom), featured Trash Clean Up Kit ($29.99), category chips, 2-column product grid (Kit $29.99 under Kits/All; Tote $3 / Grabber $23.99 / Child Vest $9.99 / Adult Vest $12.99); Shop tab active; Home/Account/Sessions nav wired to `/shop`.
- **Session detail (`/session-detail`)** — native `SessionDetailScreen` (Figma `session_detail`, `515:1848`): `useSessionDetail` fetches from Fly API + signed Supabase Storage photo URLs; walking-path map via `SessionRouteMapPanel` + `useSessionRouteCoordinates` with icon **Play / Pause / Replay** controls and synced replay timer; auto-replays once on load (skipped when reduced motion is enabled) on the basemap layer selected when the session ended (`CompletedSessionSnapshot.mapLayer`); pan/zoom when route exists; status badge, title/date/address, hours/miles/photos stats, shared `SessionPhotosSection` **Photos** carousel matching post-session confirmation (horizontal selfie/progress thumbs with time pills; tap → full-screen enlarge); read-only **Description** (setup text) and editable **Notes** (500-char limit with live counter, persisted locally via `sessionNotesStore` — editable when revisiting from Sessions list). **Delete session** (non-approved only) calls `DELETE /sessions/:id` when synced and clears local recent/cache/stats + AsyncStorage tombstones; **Download PDF** when approved (Fly service letter + `expo-sharing`). **New Session** CTA when letter download is shown uses outlined secondary styling.
## Design ground truth

- **Figma** is now the authoritative design source — [CleanUpGiveBack file](https://www.figma.com/design/DrDcQH14n7ntDQ80F7au9S/CleanUpGiveBack?node-id=1-3) holds 7 flow pages (6 designed + 1 compliance spec), 46 manifest screens, and a Design System page (104 variables, 14 text styles).
- Local workspace: [`frontend/design/figma/`](../frontend/design/figma/README.md) — screen manifest, per-flow notes, **token JSON** in [`tokens/`](../frontend/design/figma/tokens/).
- Runtime tokens: [`frontend/src/constants/tokens.ts`](../frontend/src/constants/tokens.ts) (canonical); `theme.ts` chrome + feature `tokens.ts` re-exports consume it.
- **Privacy & compliance** documented in [`docs/compliance/`](../docs/compliance/privacy-and-data-protection.md) — COPPA under-13 block with signup PII wipe; universal highest-privacy defaults (opt-in notifications, no teen tier); additional Figma compliance screens designed but not all shipped. In-app Privacy Policy (`/privacy-policy` + four detail routes) uses draft copy effective **July 20, 2026** / last updated **July 23, 2026 at 5:17 PM CDT** from [`privacyPolicyContent.ts`](../frontend/src/features/figma-screens/content/privacyPolicyContent.ts) (MapLibre/CARTO/Esri, Fly.io, Resend — not Google Maps as a processor); outline in [`mobile-app-privacy-policy-outline.md`](../docs/compliance/mobile-app-privacy-policy-outline.md).
- The Stitch/HTML pipeline is **frozen**. No new screens will be added to `HTML_MAP` or to Stitch.
- Migration is tracked per-screen in [`frontend/design/figma/manifest.yaml`](../frontend/design/figma/manifest.yaml): `designed` → `bound` → `implemented`.

## Sessions + geolocation (implemented — Expo Go test phase)

- **Supabase anonymous auth** on app launch via `AuthProvider` in `_layout.tsx` (`frontend/src/lib/supabase.ts`)
- **Fly sessions API** client (`frontend/src/lib/api.ts`, `sessionsApi.ts`) — persists create/checkpoint/finalize when `EXPO_PUBLIC_API_URL` is set. **Finalize is awaited with retry (2026-08-04):** `finalizeLiveSession()` retries the finalize POST up to twice with backoff before giving up (previously fire-and-forget, so a failed sync was silently invisible and the session never reached Supabase); `SubmissionConfirmationScreen` shows a **Retry Sync** banner (`getLastFinalizeSyncFailed`/`retryFinalizeSync`) when it still fails after retries, instead of the blanket "submitted for approval" message
- **GPS + camera** live in Expo Go via `liveSessionStore` (`expo-location` BestForNavigation at **1 s / ~1 m**; 2D Kalman position filter; route append gated on **Kalman-resolved accuracy ≤ 25 m**; stationary gate ignores `speedMps === 0` (common on iOS while walking) and uses distance from last route point; foreground/background dedupe; hardened capture filters — min-move `max(2.5m, accuracy×0.45)`, slow-walk floor **0.14 m/s**, gap recovery, sharp-reversal rejection; mid-session foreground resume preserves Kalman via soft subscription stop; device-compass heading with adaptive EMA + platform accuracy gating and GPS fallback; EMA-smoothed live arrow; live tracker **My Location** control (flyTo + optional follow ~280 ms); **`displayRouteCoordinates` + live tip refreshed every fix** (tip extends only after ≥2 committed route points — no seed→pin fake line while stationary); WebView MapLibre line layer re-ensured each update (`layout` line-join/cap); distance UI shows hundredths under 0.1 mi; lighter live display simplify (~1 m + tail) + tip segment on live maps; **session detail / confirmation replay** uses the same **`simplifyRouteForLiveDisplay`** (not 4 m preview simplify); Douglas-Peucker on legacy 4 m paths where still referenced; live maps wait for first GPS fix with “Getting precise location…” instead of US overview flash). **Live session draft** persists to AsyncStorage while active; **Resume/Discard** on cold start via `LiveSessionResumeGate`. **Background GPS** during active sessions via `expo-task-manager` when Always location is granted (EAS dev build required; **Expo Go is foreground-only** — GPS restarts when the app returns to foreground and re-asserts background task when Always granted; route gaps while locked/backgrounded are expected in Expo Go)
- **WebView map** in Expo Go (MapLibre GL JS + Carto Voyager / Dark Matter) per ADR-005; user pan/zoom on live and preview maps; live tracker map layer picker (Standard / Satellite / Hybrid) wired to `liveSessionStore.mapLayer`; Standard light/dark theme via `mapThemeStore` (auto by local time 7pm–6am, Account toggle to follow time of day, live sun/moon tool to override); **tracker UI chrome** (timer/location/tools/checkpoint/CTAs/Map Types) follows the same theme via `trackerChromeTheme.ts`; dark theme repaints parks/nature reserves/green landcover a legible dark green (upstream Dark Matter renders them invisible against the near-black background — see `mapStyles.ts`); weather pill uses Open-Meteo `weather_code` icons; native MapLibre unchanged for EAS builds
- **Sessions list** refetches from Fly API on focus when `EXPO_PUBLIC_API_URL` is set — loading state + empty CTA (**No sessions logged yet.** / **Log session?** → session setup guide); when API is unset, shows filtered mock rows; **Select** → bulk delete non-approved sessions and **Download PDF** for selected approved sessions (AsyncStorage tombstones keep deleted ids hidden across restarts)
- **Session detail** fetches from Fly API + Supabase Storage signed URLs via `useSessionDetail`; volunteers can **Delete session** when status is not `approved`; **Download PDF** when `approved` (`GET /sessions/:id/service-letter.pdf` via `downloadServiceLetterPdf` + share sheet); after delete, returns to `/sessions-list` with row removed
- **Checkpoint grace** — **5 minutes** after a due checkpoint (`CHECKPOINT_MISS_GRACE_MS`) before invalid finalize / `/missed-checkpoint`
- **Checkpoint upload recovery (2026-08-07)** — each `PhotoCheckpointSubmission` tracks a `syncStatus` (`pending`/`synced`/`failed`); photos are always saved to persistent on-device storage before upload (`persistCheckpointPhotos.ts`), and `/live-session` shows a per-checkpoint "Saved on this device" indicator + targeted **Retry upload** button (`retryCheckpointSync`) instead of only the generic sync-warning banner
- **Backend:** Fastify + Prisma in `backend/sessions/` — deployed to `https://cleanup-sessions.fly.dev`; uses Supabase Postgres + JWKS (ES256) auth verification; **service letter PDF** via `@react-pdf/renderer` + OSM static maps (`GET|POST …/service-letter.pdf`); letter assets under `backend/sessions/assets/`
- **GPS trail QA:** Outdoor Expo Go walk confirmed green polyline + distance increments after stationary-gate / WebView layer / distance-format fixes (2026-07-21). EAS + Always lock-screen continuity still open.
- **Checkpoint / finalize sync:** Client **recreates remote session once and retries** on Fly `404 Active session not found` for both checkpoints (`ensureRemoteSession`) and finalize (`createRemoteSessionFromSetup` — works after local teardown). Mid-session sync failures set `sessionSyncWarning`, shown as a banner on `/live-session` (cleared on successful checkpoint sync). Fill `frontend/.env` with **project root** Supabase URL (no `/rest/v1/`) and **anon public JWT** — see [supabase.md](supabase.md). Restart Metro after `.env` changes. Residual 404s usually mean API/env mismatch — see [expo-go-eas-tester-runbook.md](frontend/specs/expo-go-eas-tester-runbook.md).
- **Feedback** — `/session-feedback` (post-session) and `/give-feedback` (Account) share `FeedbackScreen` (rating row **Very Sad → Excited**); Submit → `/feedback-thank-you` (`dismissTo` confirmation or Account). Persisted end-to-end: `POST /feedback` (Fly, `feedbackApi.ts`) inserts into `public.volunteer_feedback`, read back on admin's `/feedback` page.

**API:** `EXPO_PUBLIC_API_URL=https://cleanup-sessions.fly.dev` in `frontend/.env`. Fly secrets deployed today: `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_JWT_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`, `DONNA_EMAIL`. Add `SUPABASE_SERVICE_ROLE_KEY` / `ADMIN_API_KEY` when letterhead/admin API needs them. Local Prisma uses `backend/sessions/.env` (session pooler `DATABASE_URL`). Redeploy Fly after service-letter PDF changes.

**Transactional email (Resend):** Domain verified; admin approve/decline + notify-at-risk (local `.env.local`); Fly event-registration / email-change OTP / Donna “session ready for review”. Soft-skips only when the key is unset. Ops: [admin/dulf-resend-supabase-fly.md](admin/dulf-resend-supabase-fly.md).

## Not implemented yet

- Supabase dashboard: enable Anonymous auth + run `sql/supabase-init.sql` storage policies if bucket missing (verify if not done)
- Production auth (email OTP), **Stripe** (shop/donate/tracker checkout — UI mock only; `backend/payments/` empty), Vercel Resend env for production admin mail
- **Shop shipping/tracking UX** — admin can paste carrier + tracking on `shop_orders`, which now sends a shipped-notification email (2026-08-07, `email-templates.ts`); live Order History track link + label purchase API still **not built**. Research brief for Donna: [research/shipping-integration-2026-08.md](research/shipping-integration-2026-08.md) (Phase 1 Pirate Ship + Resend → Phase 2 Shippo)
- Detach unused Fly Managed Postgres cluster (`cleanup-sessions-db`) if provisioned during `fly launch` — app uses Supabase Postgres

## How to run

```bash
npm install --prefix frontend
npm start              # smart: hotspot → LAN; else tunnel (cellular / guest Wi‑Fi)
npm run start:lan      # same Wi‑Fi (fast)
npm run start:hotspot  # Mac on iPhone Personal Hotspot → LAN
npm run start:device   # force tunnel (phone on LTE/5G)
```

Full matrix and troubleshooting: [expo-go-dev-networking.md](frontend/specs/expo-go-dev-networking.md).

**Physical device (Expo Go):**

| Phone connection | Command |
|------------------|---------|
| Same Wi‑Fi as Mac | `npm run start:lan` |
| Mac on iPhone hotspot (`172.20.10.x`) | `npm start` or `npm run start:hotspot` |
| Phone on cellular, Mac on Wi‑Fi | `npm run start:device` |

Metro clears inherited `CI=true` so fast refresh stays enabled. If tunnel/ngrok times out, the start script prints recovery commands.

### EAS development build (background GPS + native MapLibre)

See [accounts-and-access.md](accounts-and-access.md). After installing a development build: `cd frontend && npm run start:dev-client`. **Rebuild required** after `app.json` / `expo-task-manager` plugin changes. Checkpoint photos use **`expo-camera`** in both Expo Go and the dev client.

## Key paths

| Area | Path |
|------|------|
| App routes | `frontend/src/app/` |
| Bundled HTML screens | `frontend/assets/stitch/` *(legacy — frozen)* |
| Figma design workspace | `frontend/design/figma/` |
| Design exports (legacy) | `frontend/design/stitch_htmls/` |
| TS prototype (reference) | `frontend/prototype/` |
