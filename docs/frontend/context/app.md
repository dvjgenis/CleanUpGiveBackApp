# Context: app

Expo Router navigation and screen entry points.

## Purpose

`frontend/src/app/` defines file-based routes. The app currently ships a **WebView HTML prototype** under `/prototype/[screen]`.

## Routes

| Route | File | Notes |
|-------|------|-------|
| `/` | `index.tsx` | Splash (`AppSplashScreen`) then **Redirect to `/welcome`** until onboarding is marked complete; afterward Home dashboard (`HomeScreen`, Figma `406:291`) — Service Hours / streak / Your Impact from `sessionStatsStore` (local snapshots + Fly API hydration via `checkpointCount`/`photoCount`); recent sessions from `recentSessionsStore`; empty until first session ends; Recent Sessions **View All** → `/sessions-list`; **three** recent events (tap → `/event-detail?id=…`); bell opens `/notifications`; **Shop** tab opens `/shop`; **Sessions** tab opens `/sessions-list`; **Profile** tab opens `/account` |
| `/welcome` | `welcome.tsx` | Welcome / login (Figma `112:6776`) — hero, **501(c)(3)** nonprofit blurb, email/password, Log In → home, Create an Account → `/create-account` |
| `/create-account` | `create-account.tsx` | Create account (Figma `105:2`) — Name/Email/Password + Apple/Google/Email social; Create Account validates then → `/creating-account`; social CTAs skip validation (prototype) → `/creating-account` |
| `/creating-account` | `creating-account.tsx` | Creating account interstitial (Figma `137:73`) — **2** rotating Did-you-know facts + ~7.5s linear progress bar; replace → `/account-phone` |
| `/account-phone` | `account-phone.tsx` | A few details — **full legal name** (immutable after setup; contact admin to change) + phone (Figma `712:323`); subtitle: accurate details + court-acceptance responsibility; Continue → `/account-details` |
| `/account-details` | `account-details.tsx` | A few details — birthday (type MM/YYYY or wheel picker) + service type (Figma `112:6882`); same page description (accurate details + court acceptance); Continue → `/location-permission` or `/under-age` if 13 or younger |
| `/location-permission` | `location-permission.tsx` | Allow location? (Figma `725:553`, step 3/5) — decorative pin illustration; Enable calls `expo-location` `requestForegroundPermissionsAsync()`; buttons top→bottom: Enable / Previous / Not now; Enable / Not now → `/camera-permission`; Previous → back |
| `/camera-permission` | `camera-permission.tsx` | Allow camera? (Figma `725:613`, step 4/5) — decorative camera illustration; Enable calls `expo-camera` `requestCameraPermissionsAsync()`; buttons top→bottom: Enable / Previous / Not now; Enable / Not now → `/notification-preference`; Previous → back |
| `/notification-preference` | `notification-preference.tsx` | Stay updated (Figma `112:7130`, step 5/5) — preference chips are display-only (`chipBg` `#f0edec` + outline, not tappable); footer pinned like account-details (`flexGrow` + `space-between`): Enable / Previous / Not now → `/setup-complete` |
| `/setup-complete` | `setup-complete.tsx` | Account created success (Figma `133:93`) — static reimported checkmark (`AccountCreatedCheck`); lime corner blobs drift slightly toward opposite corners on enter; copy + Continue fade/slide in; subtitle uses `color/text/on-primary`; Continue marks onboarding complete → `/home-tour` |
| `/home-tour` | `home-tour.tsx` | Tour step 1 (Figma `137:527`) — home impact preview; prefetches shop/track webp graphics; Continue → `/shop-tour` |
| `/shop-tour` | `shop-tour.tsx` | Tour step 2 (Figma `137:115`) — shop gear preview (`shop-showcase.png` via expo-image; mint kit card + transparent corners on forest-green bg); Continue → `/track-tour` |
| `/track-tour` | `track-tour.tsx` | Tour step 3 (Figma `137:431`) — live map preview (`track-map.webp` via expo-image); Continue → `/session-tour` |
| `/session-tour` | `session-tour.tsx` | Tour step 4 (Figma `137:173`) — previous-sessions list graphic (~95% width, raised); Continue → `/set-tour` |
| `/set-tour` | `set-tour.tsx` | Tour finale (Figma `112:7170`) — speckled lime stars (`star-set.svg`) orbit copy; empty on arrival, then staggered fade-in after ~450ms; Replay → `/home-tour`; Start Tracking → `/session-setup-guide`; Go Home fades the screen out then → `/` with `enter=fade` + home opacity fade (`homeEnterTransition`) |
| `/under-age` | `under-age.tsx` | Parent/admin permission gate (Figma `728:901`); Contact Admin mailto; **Learn why** → `/under-age-learn-why` |
| `/under-age-learn-why` | `under-age-learn-why.tsx` | Why admin approval is required (Figma `833:314`); four reason cards + Contact Admin; Back → `/under-age` |
| `/shop` | `shop.tsx` | Shop home (`ShopScreen`, Figma `498:606` / PRD §6.19) — mission copy, donate card ($5/$10/$15/Custom → `/donate?amount=`), featured Trash Clean Up Kit (**View Kit** → `/product-detail?id=cleanup-kit`), category chips (All/Kits/Tools/Safety/Bags; **Kits** grid shows Trash Clean Up Kit), 2-column product grid (**View** → `/product-detail?id=` per SKU); **Add to cart** updates shared `cartStore` (starts empty); cart badge count = total qty; empty cart icon → toast; cart with items → `/cart`; Shop tab active; catalog images boot-prefetched via `shopAssets.ts` (`expo-image` memory-disk, `transition={0}`) |
| `/donate` | `donate.tsx` | Contribute / Donate (`DonateScreen`, Figma `412:4` / PRD §6.20) — amount presets + custom; **Continue** → `/purchase-confirmation?mode=donation&amount=`; back → Shop |
| `/product-detail` | `product-detail.tsx` | Product detail (`ProductDetailScreen`, Figma `492:114` + SKU frames / PRD §6.21) — parameterized `?id=`; quantity stepper + **Add to cart** → `cartStore`; empty-cart toast on cart icon; cart badge reflects live count; back → Shop |
| `/cart` | `cart.tsx` | Cart (`CartScreen`, Figma `657:1585` / PRD §6.22) — line items + donation from `cartStore` (default $5), order summary, **Continue** → `/checkout`; back → previous |
| `/checkout` | `checkout.tsx` | Checkout (`CheckoutScreen`, Figma `657:1809` / PRD §6.23) — order summary from `cartStore` (same items + donation), shipping + payment forms; **Place Order** validates required fields → `/purchase-confirmation`; **`?mode=tracker`** — fixed $49.99 tracking access (includes kit + shipping), title "Payment", no cart icon; Place Order → `/purchase-confirmation?mode=tracker&returnTo=` + sets `trackerPaymentStore` |
| `/purchase-confirmation` | `purchase-confirmation.tsx` | Confirmation (`PurchaseConfirmationScreen`, Figma `494:262`) — order mode: cart lines; donation mode (`?mode=donation&amount=`): gift-only receipt; **`?mode=tracker&returnTo=`** (Figma `1168:3619`): tracker access line + "Estimated Shipping: ~2-3 days" detail row, single **Continue** button → `returnTo` (default home); hearts animate; **Continue Shopping** / **Back to Shop** / **Go Home** for order/donation modes |
| `/event-detail` | `event-detail.tsx` | Event detail (`EventDetailScreen`, Figma `196:226`) — UPCOMING badge only (no registered count); what-to-bring: weather-appropriate clothing + CUGB provides snacks/supplies/water/music; **Register** emails confirmation via `POST /emails/event-registration` when API configured + success modal |
| `/sessions-list` | `sessions-list.tsx` | Sessions list (`SessionsScreen`) — API or multi-status `mockSessionsList`; sort-row **Select** → bulk delete non-approved |
| `/session-detail` | `session-detail.tsx` | Session detail (`SessionDetailScreen`, Figma `515:1848` / PRD §6.18) — map hero with `SessionRouteMapPanel` **Play / Pause / Replay** (length-scaled duration; auto-play skips when reduced motion), status, stats, shared `SessionPhotosSection` **Photos** carousel (same as post-session confirmation: horizontal selfie/progress thumbs with time pills; tap → edge-to-edge `PhotoEnlargeModal`), read-only **Description** + editable **Notes** (500-char limit, `sessionNotesStore`); sticky footer matches Event Detail: outlined **Delete session** when status ≠ `approved` (`removeVolunteerSession` + tombstone ids); success → `replace('/sessions-list')`; **New Session** primary CTA via `RegisterButton`; extra scroll bottom pad (+96 beyond footer) clears the fixed CTA; back returns to list |
| `/account` | `account.tsx` | Account tab (`AccountScreen`) — Personal Details; Records / Shop / Preferences / Permissions; **Company code** section at bottom (confirm → `markTrackerPaid` + upgrade modal) |
| `/personal-details` | `personal-details.tsx` | Personal Details — **email only editable** (OTP via `/emails/email-change/*`); other fields grayed; contact admin to change legal name |
| `/give-feedback` | `give-feedback.tsx` | Account Give Feedback — same `FeedbackScreen` UI as session-end, with title **"We'd love your feedback!"** (session route keeps **"Rate your experience!"**); typing bubbles fade small → medium → big; Submit → `/feedback-thank-you?returnTo=account` → Account; Skip → back to Account |
| `/delete-account-confirm` | `delete-account-confirm.tsx` | Delete account confirmation (`DeleteAccountScreen`, Figma `725:361`) — type **DELETE** to confirm; invalid confirm shows session-setup-style toast; back returns to Account |
| `/order-history` | `order-history.tsx` | Order History (`OrderHistoryScreen`, Figma `854:116`) — order cards with Delivered status + email receipt chips; back → Account |
| `/donation-history` | `donation-history.tsx` | Donation History (`DonationHistoryScreen`, Figma `854:205`) — donation amount cards + email confirmation chips; back → Account |
| `/approval-history` | `approval-history.tsx` | Approval History (`ApprovalHistoryScreen`, Figma `854:294`) — summary stats + session cards with Approved / Under Review / Not Approved; back → Account |
| `/export-service-record` | `export-service-record.tsx` | Export Service Record — timeframe, **Court mandated** (approved-only), status multi-select, PDF/CSV |
| `/export-record-success` | `export-record-success.tsx` | Export success (`ExportRecordSuccessScreen`, Figma `840:561`) — checkmark card; Continue → `/account`; View PDF/CSV placeholder |
| `/request-data` | `request-data.tsx` | Request your data (`RequestDataScreen`, Figma `728:1385`) — Access / Delete / Download radio options; Submit → `/request-data-sent` |
| `/request-data-sent` | `request-data-sent.tsx` | Data request sent confirmation (`RequestDataSentScreen`, Figma `728:1648`); Continue → `/account` |
| `/notifications` | `notifications.tsx` | Notification preferences (`NotificationsScreen`, Figma `649:774`) — toggle categories, back returns to home; Sessions → `/sessions-list`; Profile → `/account` |
| `/session-setup-guide` | `session-setup-guide.tsx` | Session setup guide step 1 — "How does this work?" + **nighttime cleanings are not allowed**; pills via `useSessionSetupGuidePillProgress('guide')` |
| `/session-setup-step2` | `session-setup-step2.tsx` | Session setup guide step 2; pills via `useSessionSetupGuidePillProgress('step2')` |
| `/session-setup-step3` | `session-setup-step3.tsx` | Session setup guide step 3 — "Time is ticking..." (Figma `229:351`); pills via `useSessionSetupGuidePillProgress('step3')` |
| `/session-setup-step4` | `session-setup-step4.tsx` | Session setup guide step 4 — "Checkpoint photos for verification." (Figma `249:369`); pills via `useSessionSetupGuidePillProgress('step4')` |
| `/session-setup-step5` | `session-setup-step5.tsx` | Session setup guide step 5 — session summary / manual verification + court-acceptance responsibility; Continue → `/session-free-hour` |
| `/session-setup-complete` | `session-setup-complete.tsx` | Session setup finale — "That's it! Let's get tracking!"; **Start Tracking** → `/session-setup` (form first) |
| `/session-setup` | `session-setup.tsx` | Session setup form (Figma `260:1312` / PRD §6.9); nighttime cleanings note; **Start Session** stashes form → `/photo-capture?mode=session-start` |
| `/session-free-hour` | `session-free-hour.tsx` | Session setup guide — `FreeHourScreen` "One free hour!" (Figma `1125:360`); `SessionSetupGuideNavRow` back chevron dismisses to the tab/screen captured when `/session-setup-guide` opened (pre-Track entry); pills via `useSessionSetupGuidePillProgress('free-hour')` (active 6; total shrinks when location/camera will auto-skip); Continue → `/session-free-kit`; Skip → `/session-setup-step6` |
| `/session-free-kit` | `session-free-kit.tsx` | Session setup guide — `FreeKitScreen` "Free cleanup kit!" (Figma `1126:451`); `SessionSetupGuideNavRow` back chevron dismisses to the pre-Track entry screen; pills via `useSessionSetupGuidePillProgress('free-kit')` (active 7; total shrinks when location/camera will auto-skip); Continue/Skip → `/session-setup-step6` |
| `/session-setup-step6` | `session-setup-step6.tsx` | Session setup guide step 6 — location permission (Figma `728:639` / `permission-location`); copy explains Always-while-session-active for locked-screen tracking; **Enable location** calls `requestSessionLocationPermission()` (foreground; Always requested at live-session start) |
| `/session-setup-step7` | `session-setup-step7.tsx` | Session setup guide step 7 — camera permission (Figma `728:658` / `permission-camera`); **Enable camera** calls `expo-camera` `requestCameraPermissionsAsync()` (iOS/Android system dialog) |
| `/live-session` | `live-session.tsx` | Live session tracker (Figma `251:439` / PRD §6.11); timer card has a subtle pulsating lime border (no IN PROGRESS badge); unpaid sessions show a smaller forest-green free-hour countdown inside the timer card (`FREE_TRIAL_DURATION_SECONDS`, default 3600) → `/free-trial-done` when expired; company code / checkout sets `trackerHasPaid` to hide countdown; **End Session** only (no manual Submit Photo) → `/photo-capture?mode=session-end`; when the 30-min checkpoint countdown hits 0, plays alert sound + haptic buzz (`alertPhotoCheckpointDue`, repeats every 45s during grace) then pushes `/photo-checkpoint`; **5‑min** grace then miss → `finalizeLiveSession({ status: 'invalid' })` + `/missed-checkpoint`; overlay chrome follows map light/dark via `getTrackerChromeColors`; map waits for a GPS seed before mounting (no US overview flash); location watch starts before Always permission / timed one-shot fix so the spinner cannot hang; stack push uses `animation: 'slide_from_bottom'` with `animationTypeForReplace: 'pop'`; back chevron → `router.replace('/')`; location pill shows live city + temperature via `expo-location` + Open-Meteo with condition glyph (`WeatherConditionIcon`); GPS via `liveSessionStore` (Kalman + adaptive gates; background task when Always granted) + MapLibre (style switch keeps markers mounted; marker has soft drop shadow); navbar compass from store `currentHeading` (compass EMA, GPS fallback); layers → `MapTypesSheet`; sun/moon → `mapThemeStore`; checkpoint card shows overlapping rounded-square photo thumbnails (`radius.sm`) — tap opens `PhotoEnlargeModal` (selfie + progress gallery, date/time, `1/N` counter, asset chevrons) |
| `/free-trial-done` | `free-trial-done.tsx` | Free-hour paywall overlay (`FreeTrialModal`, Figma `1141:2178`) — `transparentModal` over live tracker; Continue **`replace`s** → `/checkout?mode=tracker&returnTo=live-session` (full stack screen, not nested modal); Pay Later → `router.back()` |
| `/photo-checkpoint` | `photo-checkpoint.tsx` | Photo checkpoint prompt (Figma `364:115` / PRD §6.12) — solid dark scrim (no park background); opened when the 30-min timer expires (sound + haptic); **Take Photo** → `/photo-capture` (no mode param — in-session checkpoint) |
| `/photo-capture` | `photo-capture.tsx` | Sequential dual capture. **`?mode=session-start`**: consumes pending setup form → `startNewLiveSession` + first checkpoint → `/live-session`. **session-end** / checkpoint modes unchanged. |
| `/photo-submitted` | `photo-submitted.tsx` | Photo submitted confirmation (Figma `260:1571` / PRD §6.12) — solid dark scrim (no park background); hero Camera Pop-Up `.lottie` inside the bordered card (`PhotoSubmittedHeroVideo` → repeater-baked `photo-submitted.lottie`); resets checkpoint countdown on mount, live `MM:SS` timer ticks until **Continue Tracking** (`dismissTo('/live-session')` so the original map is restored, not a nested replace); shows session photo count only when the just-submitted photo was taken early (before countdown hit zero) |
| `/submission-confirmation` | `submission-confirmation.tsx` | Session detail / submission confirmation (Figma `269:1615` / PRD §6.15) — re-reads `getCompletedSessionSnapshot()` on focus (`useFocusEffect`) for checkpoint photos + route; shared `SessionPhotosSection` **Photos** carousel; `SessionRouteMapPanel` replay (WebView in Expo Go via `isExpoGoClient()`); **Go Home** → `/` with `enter=fade` + `homeEnterTransition` |
| `/missed-checkpoint` | `missed-checkpoint.tsx` | Missed checkpoint error (Figma `269:1587` / PRD §6.13) — solid dark scrim (no park background); reached after grace miss invalidates the live session; hero Lottie loops continuously |
| `/prototype/[screen]` | `prototype/[screen].tsx` | Renders Stitch HTML by screen key |
| Layout | `_layout.tsx` | Root `Stack` wrapped in `AuthProvider` (Supabase anonymous sign-in on launch); mounts `LiveSessionResumeGate` (cold-start Resume/Discard for AsyncStorage live-session drafts); imports `backgroundLocationTask` so TaskManager registers before sessions start; boot-prefetches tour + session-setup/onboarding + shop graphics (`prefetchAllTourGraphics` + `prefetchAllOnboardingGraphics` + `prefetchAllShopGraphics`); explicit `Stack.Screen` entries for native session routes, shop + donate + product-detail, account, event-detail, notifications, sessions-list + prototype; tab roots (`index`, `shop`, `sessions-list`, `account`) use `animation: 'none'` (BottomNav instant); `/` accepts `?enter=fade` for tour / submission-confirmation Go Home cross-fade; imports `global.css` for NativeWind (MapLibre map shell) |

## Patterns

- **Live session crash recovery:** while `isActive`, `liveSessionDraft.ts` debounces an AsyncStorage snapshot; on next cold start `LiveSessionResumeGate` offers **Resume** (restores store + pushes `/live-session`) or **Discard** (clears draft; does not PATCH mid-session route to Fly)
- **Track tab** (`BottomNavBar` on home, sessions, account, notifications): active live session → `/live-session`; otherwise → `/session-setup-guide` (guide → form → session-start photos → live)
- **Session start → tracker back:** photo submit after form starts the session and replaces → `/live-session?from=onboarding`; tracker back then `replace('/')` (home + minimized bar).
- **Shop tab** → `/shop`; **Sessions tab** → `/sessions-list`; **Profile tab** → `/account`
- **Tab switches** use `animation: 'none'` (instant); tour and submission-confirmation Go Home use `?enter=fade` + home opacity fade; hierarchical pushes keep the default slide
- **Guide navigation:** Continue uses `router.push`; Previous/back use `router.back()` (reverse swipe). Step 6 Previous uses `router.replace('/session-setup-step5')` with `animationTypeForReplace: 'pop'` when Skip bypasses the stack.
- **Guide progress pills:** Coachmarks through free-kit use `useSessionSetupGuidePillProgress` with linear active indices (1–7). When location/camera will auto-skip, **total** shrinks (8 vs 10) so the bar stays contiguous without starting the guide on pill 3 (e.g. both granted: step5=5/8 → free-hour=6/8 → free-kit=7/8 → complete=8/8).
- **Onboarding tour:** setup-complete → `/home-tour` → `/shop-tour` → `/track-tour` → `/session-tour` → `/set-tour` (Continue / Previous); finale Replay / Start Tracking / Go Home
- **Onboarding account flow:** create-account → creating-account → account-phone → account-details → location-permission → camera-permission → notification-preference → setup-complete (under-age branch from account-details when age ≤ 13; users 14+ proceed straight through, no parent-permission gate)
- **NAV_RULES** + injected JS bridge handle cross-screen navigation via `postMessage`
- Metro bundles `.html` via `frontend/metro.config.js` (`assetExts` includes `html`)
- Asset loading: `expo-asset` + `expo-file-system` with fetch fallback

## Decisions

- Stack navigator over `NativeTabs` so `/prototype/*` routes are reachable (see [progress.md](../../progress.md))
- Tab-root screens stay instant on BottomNav; `/set-tour` Go Home fades the finale out first, then home opacity-fades in via `?enter=fade` + `homeEnterTransition` (stack param alone is not enough)
- `injectedJavaScriptBeforeContentLoaded` + `onMessage` for iOS WKWebView navigation
- Phone country picker uses Unicode flag emojis + dial codes from `constants/countries.ts` (no flag asset package); US formatting applies for US/CA only; modal scrim is static while only the sheet springs up/down (`animationType: none`)
- Phone step collects **full legal name** first (required, min 2 chars; immutable after setup) then phone; legal name is saved to `onboardingStore` and shown in the home greeting
- `onboardingStore` also persists phone, birthday, service type, and email; Account **Personal Details** allows **email-only** edits (OTP verification); other fields are read-only
- Recent event card images use mock location-mapped remote URIs (`eventImageForLocation` in `mocks/home.ts`)
- Phone step uses `KeyboardAvoidingView` + tap-outside / drag-to-dismiss so Continue stays reachable above the phone-pad keyboard
- `CheckoutScreen` form fields chain focus via `TextInput` refs + `returnKeyType="next"` / `onSubmitEditing` (Full Name → Street → City → State → ZIP → Card Number → Expiry → CVV → Name on Card); Name on Card is `returnKeyType="done"` and submits the order. CVV is capped at 3 digits (`maxLength={3}`, `slice(0, 3)`).

## Policies

- **Minimum age is 13.** `AccountDetailsScreen` blocks Continue to `/under-age` (the parent/admin permission gate) only when `ageFromMonthYear(birthday) <= 13`. Users age 14+ go straight to `/location-permission` — the parent-permission screens never show for them.
- **Do not add new screens to the prototype.** New screens are registered in `frontend/design/figma/manifest.yaml` and implemented as native RN routes under `src/app/`.
- The HTML prototype (`/prototype/[screen]`) is **frozen** — it continues to run for Expo Go demos but receives no new features.
- Native production screens live under `src/app/<routeKey>.tsx` following Expo Router file-based conventions. Use the `routeKey` from `manifest.yaml` as the filename.
- When a native screen ships, set its `status` to `implemented` in `manifest.yaml` and remove its entry from `HTML_MAP`.
